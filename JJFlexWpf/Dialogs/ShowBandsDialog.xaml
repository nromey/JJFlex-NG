<local:JJFlexDialog x:Class="JJFlexWpf.Dialogs.ShowBandsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JJFlexWpf"
        Title="Show Bands"
        Width="450" Height="420"
        ResizeMode="CanResizeWithGrip">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Show" MinWidth="80" Height="28" Margin="0,0,8,0"
                    IsDefault="True" Click="ShowButton_Click"
                    AutomationProperties.Name="Show band info" />
            <Button Content="Cancel" MinWidth="80" Height="28"
                    IsCancel="True"
                    AutomationProperties.Name="Cancel" />
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Column="0">
                    <Label DockPanel.Dock="Top" Content="Band" Padding="0,0,0,4" />
                    <ListBox x:Name="BandBox" Height="120"
                             AutomationProperties.Name="Band" />
                </DockPanel>
                <DockPanel Grid.Column="2">
                    <Label DockPanel.Dock="Top" Content="License" Padding="0,0,0,4" />
                    <ListBox x:Name="LicenseBox" Height="120"
                             AutomationProperties.Name="License class" />
                </DockPanel>
                <DockPanel Grid.Column="4">
                    <Label DockPanel.Dock="Top" Content="Mode" Padding="0,0,0,4" />
                    <ListBox x:Name="ModeBox" Height="120"
                             AutomationProperties.Name="Mode" />
                </DockPanel>
            </Grid>
            <DockPanel Grid.Row="1">
                <Label DockPanel.Dock="Top" Content="Result" Padding="0,0,0,4" />
                <TextBox x:Name="ResultBox" IsReadOnly="True"
                         TextWrapping="NoWrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         AutomationProperties.Name="Band information result" />
            </DockPanel>
        </Grid>
    </DockPanel>
</local:JJFlexDialog>
