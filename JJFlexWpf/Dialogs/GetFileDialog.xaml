<local:JJFlexDialog x:Class="JJFlexWpf.Dialogs.GetFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JJFlexWpf"
        Title="Select File"
        Width="1" Height="1"
        WindowStyle="None" ShowInTaskbar="False"
        Opacity="0">
    <!-- This is a headless dialog that wraps OpenFileDialog.
         It shows no UI of its own - just the native file picker. -->
    <Grid />
</local:JJFlexDialog>
