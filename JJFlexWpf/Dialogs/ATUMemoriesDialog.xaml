<local:JJFlexDialog x:Class="JJFlexWpf.Dialogs.ATUMemoriesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JJFlexWpf"
        Title="ATU Memories"
        Width="300" Height="160"
        SizeToContent="Height">
    <StackPanel Margin="12">
        <DockPanel Margin="0,0,0,8">
            <Label Content="ATU Memories:" DockPanel.Dock="Left" Padding="0,0,8,0"
                   VerticalAlignment="Center" />
            <ComboBox x:Name="EnableControl"
                      AutomationProperties.Name="ATU memories enabled" />
        </DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Clear Memories" MinWidth="100" Height="28" Margin="0,0,8,0"
                    Click="ClearButton_Click"
                    AutomationProperties.Name="Clear ATU memories" />
            <Button Content="Done" MinWidth="80" Height="28"
                    IsDefault="True" IsCancel="True"
                    AutomationProperties.Name="Done" />
        </StackPanel>
    </StackPanel>
</local:JJFlexDialog>
