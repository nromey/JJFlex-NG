<local:JJFlexDialog x:Class="JJFlexWpf.Dialogs.FreqInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JJFlexWpf"
        Title="Frequency Input"
        Width="300" Height="140"
        SizeToContent="Height">
    <StackPanel Margin="12">
        <DockPanel Margin="0,0,0,4">
            <Label Content="Freq:" DockPanel.Dock="Left" Padding="0,0,8,0"
                   VerticalAlignment="Center" />
            <TextBox x:Name="FreqBox"
                     AutomationProperties.Name="Frequency"
                     VerticalAlignment="Center" />
        </DockPanel>
        <TextBlock Text="Allowed forms: MHZ.KHZ.HZ, MHZ.KHZ, KHZ"
                   Foreground="Gray" FontSize="11" Margin="0,0,0,8" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" MinWidth="80" Height="28" Margin="0,0,8,0"
                    IsDefault="True" Click="OKButton_Click"
                    AutomationProperties.Name="OK" />
            <Button Content="Cancel" MinWidth="80" Height="28"
                    IsCancel="True"
                    AutomationProperties.Name="Cancel" />
        </StackPanel>
    </StackPanel>
</local:JJFlexDialog>
