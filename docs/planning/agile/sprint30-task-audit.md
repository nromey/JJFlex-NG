# Task backlog audit against the code — 2026-08-18

Audited at HEAD `972e1438` on `honest-tx-audio`. Every verdict below is grounded in the working tree as of this commit, which includes the 2026-08-17 ten-track merge and the 2026-08-18 speech-intent day. The task store (`~/.claude/tasks/b06e594e-.../`) and `docs/planning/active/research-queue.md` both lag the code — fourteen tasks marked pending are done, and several "current state" paragraphs describe code that has since changed. Counts: 14 already done, 8 partially done, 16 still valid, 3 drifted, 1 unverifiable, 0 superseded.

A note on method: where a task body was refreshed 2026-08-17/18 it is usually accurate; where it dates from 2026-08-12/13 it usually is not. The drift is concentrated exactly where the work has been heaviest.

## ALREADY DONE — 14 tasks. Do not plan tracks for these.

- **#9 — About page reporting every component version.** Done as designed. `JJFlexWpf\Dialogs\AboutDialog.xaml:27` hosts WebView2 with a plain TextBox fallback at line 33 (the task's one hard constraint, honored). Versions come from the single-assembler `Radios\DiagnosticSnapshot.cs:478-520`: .NET, FlexLib, Opus (live `opus_get_version_string`), PortAudio rendered revision-first and never a bare 19.7.0 (`PortAudioDisplay()` at 528-543), WebView2 runtime, speech backend ("Prism, using NVDA", commit `53fa5f9f`), OS, trace paths, braille. One residual crumb: the About tab carries a second, independent version assembler (`AboutDialog.xaml.cs:202-229`) that re-derives FlexLib's version — the exact two-assemblers failure the `DiagnosticSnapshot` comment at 169-178 forbids. Close the task; file the crumb as a small cleanup.
- **#26 — the four diagnostic-log questions.** Answered. `docs/planning/active/diagnostic-log-surface.md:454-484`, heading literally reads "Open questions — ANSWERED 2026-08-16". Capture chord ruled `Ctrl+J, Ctrl+D` (plain D was taken), default ON confirmed, export-on-stop confirmed, `KeepDailyTraceLogs` retired immediately. The implementation track this unblocks is #18's real remainder.
- **#32 — installer file-list verification.** Done. `build-installers.bat:103-118` rmdirs both Release trees plus stale Setup exes before building; the NSIS template excludes the litter (`install template.nsi:85-90`, `File /r /x "*.pdb" /x "*.xml"` with JJLogIO.xml/JJTrace.xml named in the comment) and deletes it on upgrade (110-114); `generate-deletelist.ps1:33-43` walks the publish tree. Harmless residual: the deleteList contains Delete lines for .pdb/.xml files the installer never ships — NSIS no-ops on missing files, but the two scripts disagree about what shipped.
- **#40 — the two help-plumbing gaps.** Both resolved. `JJFlexWpf\HelpLauncher.cs:80` maps CommandFinder to `pages/command-finder.htm`; the page exists, is in all three CHM project files, and `CommandFinderDialog.xaml.cs:65` requests it. The ampersand is a documented carve-out: `NativeMenuBar.cs:952-954` explains native Win32 menus render `&` as access keys and NVDA reads them cleanly. Residuals if anyone wants them: the carve-out comment sits on `BuildTransmitItems`, not the Help builder; CLAUDE.md's no-ampersand guideline still lacks the carve-out; and "What's &New" is the only Help item WITH a mnemonic, so the menu's letter navigation is inconsistent.
- **#49 — corrupt preset file silently becomes the defaults.** Fixed exactly as prescribed. `Radios\AudioChainPreset.cs:344-372`: parse failure sidelines the file (`SidelineCorruptFile` at 381-395, renamed to `.unreadable-{timestamp}`, never overwritten) and reports the path out; `JJFlexWpf\MainWindow.xaml.cs:127-143` speaks it at Critical: "Your saved audio presets file could not be read... The unreadable file was kept next to it as...". Fresh-install no-file still gets silent defaults, correctly.
- **#50 — presets lack schema version and TX EQ.** Both landed. `AudioChainPreset.cs:24-29`: `CurrentSchemaVersion = 1`, `[XmlAttribute("schemaVersion")]`, stamped on save (412), missing tolerated as v1 in TryLoad (229-238). TX EQ: `TxEqCaptured`, `TxEqEnabled`, eight band fields (67-77), applied 110-127, captured 155/177-186 — built on FlexLib's `Equalizer.cs` via the `FlexBase.cs:10243-10307` wrappers.
- **#51 — preset does not record which input it was tuned for.** It does now, using the exact "record and announce a mismatch without changing it" option the task weighed. `AudioChainPreset.cs:53` `RadioMicInput`, captured from `rig.MicSource` (156), mismatch note emitted on apply (132-140). Nothing reroutes the input. Close it; the PC-side half lives in #44.
- **#54 — built-in vs jack finding.** The finding is durably recorded where it protects future work: `JJPortaudio\JJPortaudio\Devices.cs:282-294` — "Built-in versus a jack is NOT claimed, and that is a finding, not an omission", with the LineLevel-for-everything evidence and the KSPROPERTY alternate route named. Nothing to build. Close the task; the code comment is the artifact.
- **#61 — default input is whatever PortAudio nominates, and nothing says so.** Substantially refuted by Track E (2026-08-16). `Devices.cs:624` sets WASAPI as the default host API with the rationale spelled out; `ResolveHostApi` (1231-1240) is the preference order; `AdoptSystemDefault` (938-958) takes the default only within the selected API rather than trusting PortAudio's nomination; host-API rows self-describe (640-663, MME's row says it "hides what rate your hardware is actually running at"); device rows speak ", system default" and name a differing host API (336-374); `AudioDevicesDialog.xaml.cs:1120-1132` warns on host-API mismatch. Both halves of the task — decide the default, and tell the operator — are done. The research queue still lists this open.
- **#68 — audioConfig.xml in two directories.** Done past what the task asked. Not write-both any more: `AudioOutputConfig.cs:284-293` `CanonicalDir` normalizes both paths to one home; Load takes a strictly-newer legacy copy and heals it forward (341-372, real migration with a log line); Save writes canonical only, deliberately (376-383); the FreqOutHandlers double-write is deleted (2823-2852). A dated sunset marker at 279-282 says to remove the legacy read in the release after 4.1.17 — that is the only crumb, and it is already scheduled in the code itself. The queue's "Load takes the newer, Save writes both" description is stale on the Save half.
- **#71 — PC audio connect setting spoken vs labelled.** Aligned. Label "As I left it" (`SettingsDialog.xaml:336`), spoken "as you left it" at connect (`MainWindow.xaml.cs:2856`), and the help page teaches the pairing explicitly (`docs/help/md/audio-troubleshooting.md:74-75`). The task's second finding — the per-radio panel invisible while disconnected — was reassigned to the #74/#75 family, which also shipped.
- **#74 — expose REM ON.** Shipped end to end, including the deferred-write wrinkle the task specified. `Radios\RadioConfig.cs:253-259` `RemOnOnConnect` (default LeaveAlone) on the serial-keyed per-radio config, editable disconnected; Settings UI at `SettingsDialog.xaml:854-858` with full wiring in `SettingsDialog.RadioProfile.cs`; applied at connect by `ApplyRemOnOnConnect()` (`MainWindow.xaml.cs:2625-2630, 2893-2910`) via FlexLib's `RemoteOnEnabled` (`Radio.cs:7995-8009`); round-trip test at `Radios.Tests\KnownRadioRosterTests.cs:109-147`. The research queue and task store both still call this open — it is the most consequential single stale entry in the backlog, since the roster-and-connect batch plan leans on it.
- **#75 — radio name invisible when the radio is discovered.** Fixed — the `PaintRoster` exclusion became a union merge. `RigSelectorDialog.xaml.cs:702-717`: the discovered-radio branch now copies `UserLabel`, `IsFavorite`, `PreferredAccount`, `PathChain` from the roster before its `continue`, with the comment "UNION, not exclusion... Root B"; row text prefers `UserLabel` (line 250). The qsy-pileup-handshake batch plan still lists this as an open symptom.
- **#76 — GPS status must lead with oscillator lock and carry PPB.** Done in the prescribed order. `Radios\FlexBase.cs:3339-3374` `BuildGpsSpokenSummary`: "Reference locked/not locked" first (3349-3351), then `FormatFreqErrorPpb` — "Clock correction N parts per billion" (3356, 3381-3387), then GPS status text, satellites, reference source, grid. Consumed by `GpsStatusDialog.xaml.cs:80-82`.

## PARTIALLY DONE — 8 tasks. Each has a shipped half and a precise remainder.

- **#18 — the tracing dialog.** The sweep fixed the frozen accessible name in the WPF dialog (`TraceAdminDialog.xaml.cs:65-77` — Content and name change together), but this audit found the fix follows a fictional state: `_isTracing` initializes false on every open and `NativeMenuBar.cs:1648-1665` never passes live `Tracing.On`, so opening the dialog mid-trace announces "Start tracing" for a trace that is already running, and pressing it restarts to a new file. No plain-language explanation of tracing or levels exists in either dialog (the level combo is five bare words). The real remainder was never the dialog: the ratified `diagnostic-log-surface.md` design retires it, and #26's answers unblocked that track. This is two tasks in one — close the dialog-polish half as overtaken (noting the fictional-state defect if the dialog survives even one more release), and open the diagnostic-log-surface implementation as its own task, folding #78 in.
- **#41 — rigmeter blame-based provenance.** The queue's "NOT started" is wrong: `tools/rigmeter/rigmeter.py` v1.2 ships an `authors` subcommand (2253-2360) — per-project plus repo-wide, vendor-excluded via `classify_path`. What is missing is the part the task calls load-bearing: the blame call at line 2285 passes neither `-w` nor `-C`, so reformats and moves reassign lines to whoever touched them last — and this codebase had a whole-tree .NET 10 migration. Remainder: add `-w -C` (consider `-C -C`), then re-check the numbers before anyone quotes them.
- **#44 — mic profiles bound to the device.** The task's "no PC-side field at all" is stale: `AudioChainPreset` now carries `PcInputDevice` (line 58, informational, populated when MicSource is PC) plus `RadioMicInput`, TX EQ and a schema version. Still missing: PC-side gain, PC NR, sample rate, and the operator/rig two-file split that is the actual design. The wiring bugs it deferred to (#45, #46) are closed. Remainder is the split itself, now with a head start.
- **#57 — selectable Opus TX rate and low-res DAX IQ.** Two features in one task, and one shipped: `FlexBase.cs:11752-11775` `OpusTxSampleRateSetting` with a validating setter, rate list `{48000, 24000, 16000, 12000, 8000}` (`AudioStream.cs:121`), UI in `AudioDevicesDialog.xaml.cs:413-435`, persisted in `AudioOutputConfig.cs`. It is operator-selectable, not adaptive. The DAX IQ half has zero app-side code — no file under Radios/JJFlexWpf/JJPortaudio references DAX IQ, and `FlexBase.cs:12041` says "We're not using DAX any more", so that half is a fresh build, not a tweak. Split the task; close the TX-rate half.
- **#58 — CW mode announce and garbled keying.** The garble is fixed: `EarconCwOutput.cs:47-52` is a single-reader Channel FIFO with one consumer loop, doc'd against BUG-057 — concurrent plays serialize instead of mixing. Exit truncation is mostly fixed: `WaitForIdle` (61-88), called from `globals.vb:3281` (1500 ms) and the 5 s SK wait in `ApplicationEvents.vb:320-334`. Remainders: the player still completes on computed duration plus 50 ms rather than observing the mixer drain (`EarconCwOutput.cs:225-232`), and the 1500 ms pre-teardown window is shorter than a 15 WPM farewell, so a slow-speed 73 can still lose its tail. On the announce side, the handler fires only in the `DemodMode` PropertyChanged arm gated on `s.Active` (`FlexBase.cs:6102-6120`), and `sliceAdded` (6952-6997) announces nothing — but FlexLib's initial property sync raises DemodMode changes per slice during connect, which is how four announcements happened; that connect-storm behavior needs one radio session to confirm under the new serialized player (where it would now play four announcements in full rather than garbling them — arguably worse). Correct the research-queue line that still calls this "speech duplication".
- **#84 — F1 context help.** The first increment shipped 2026-08-18 13:47 (`40307951`) — Ctrl+F1 walks the visual tree reading `AutomationProperties.HelpText` with an honest empty-case (`KeyCommands.cs:555-595`, binding at 1237). But "verified working" is no longer the state: by 21:05 the same day it was instrumented because it reported "no extra explanation" on every control INCLUDING ones that carry HelpText — the comment at 560-565 suspects the WinForms-Keys-routed-through-the-shell versus WPF-dialog focus boundary, where "what has focus" has more than one answer. Coverage is Settings-only anyway: 15 HelpText attributes in `SettingsDialog.xaml`, 5 SetHelpText calls in the Workshop code-behind (all mic-area), nothing else. Remainder, in order: fix the focus boundary (the trace lines are in place to answer it), then coverage, then the never-built F1 ContextMap current-context mechanism and the `keyboard-reference.md:71` doc lie. Do not schedule coverage work ahead of the boundary fix — HelpText nobody can surface is more description drift.
- **#86 — speech flow, mechanism pretending to be an intent.** The mechanism shipped: `SpeechIntent` (`Radios\Speech\SpeechIntent.cs`) plus `SpeechTier`, the lead-then-settle coalescer (`ScreenReaderOutput.cs:144, 238-370`), and a single `Emit` funnel (438-442) so no intent can bypass suppression or history. The legacy bool-interrupt overloads remain (475, 508) with roughly 300 interrupt-style call sites across 39 files. The deferred connect-cluster QUEUE conversions must NOT be executed as surveyed: commit `972e1438`'s own message records that for the disconnect announcement "Interrupt was tried, then Queue, then Interrupt again; the operator heard the new window's title and nothing else every time" — a screen reader flushes its queue on window change, and the connect sequence is nothing but window changes. The pattern that finally worked is handing the utterance to the ARRIVING window (fold it into the window's title, `PendingDisconnectLead`). Reframe the remainder: conversions near window transitions use the arriving-window pattern, not Queue; the survey's per-site recommendations are only trustworthy away from window boundaries.
- **#90 — Workshop radio controls with no radio. NOT superseded; half fixed.** The caller's brief expected supersession by the limited-Home decision; no written record of that decision exists in the repo or project memory, and it would not cover this anyway — the Audio Workshop is on the limited page's own menu, deliberately usable offline for the mic check, so gating INSIDE the Workshop survives any Home redesign. Half landed 2026-08-18: `UpdateRadioControlAvailability` (`AudioWorkshopDialog.xaml.cs:589-599`) disables the five radio-side checkboxes (MicBoost, MicBias, Compander, Processor, Monitor) when `_rig == null`, dropping them from tab order, with the defect documented at 567-588. Remainder: the radio-side VALUE controls are not gated — `_micGainControl` (789-798), `_txFilterLowControl`/`_txFilterHighControl` (914-936), `_micSourceControl` (782) have handler-only guards, stay tabbable, and `ValueFieldControl` speaks changing values on arrow keys with no rig (`ValueFieldControl.xaml.cs:166, 432, 480, 499`) — the same confident lie the checkbox fix killed. The open design question in the task (disabled versus reachable-for-inspection with "unavailable" in the name) still needs Noel. One correction: there is no radio-side noise-reduction control in the Workshop (the cleanup section is PC-side by design, 583-587), so the reported "noise reduction" example was one of the five now-gated toggles or PC NR, which is valid offline.

## STILL VALID — 16 tasks. Descriptions hold; work is outstanding.

- **#10 — Track F receiver simulation.** No receiver-sim, AGC-mirror or IQ-playback code exists in Radios or JJFlexWpf. Gated on #56's bench session, and the 1-SCU headline claim can only be validated on Don's 6300, not the bench 8600.
- **#21 — field-test the orphan fix.** The fix is confirmed in code — `FlexBase.cs:11946` background remoteAudioThread with the four-ghosts rationale, bounded Finished poll at `Audio.cs:812-843` — so only the launch/exit-cycle test at a real machine remains. A pure test task; candidate line item for #55's master list.
- **#27 — transverter bench Session One.** Plan intact at `docs/planning/active/transverter-completion-plan.md` (experiments now run past X1-X9 — X12 and X14 appear). Needs the radio and Noel. It is the transverter half of #56.
- **#29 — tone monitor clicks.** The investigation instrumentation the task called for has landed (statusFlags logging, `Audio.cs:955-1000`, first-appearance-per-flag plus close summaries), so the next repro will finally say whether PortAudio saw glitches. The bug itself is untouched and still needs the laptop voice-through-monitor test. Same family as #17 — re-measure both in one session.
- **#39 — audio-earcon-control.md describes controls that do not exist.** Confirmed uncorrected, and reality has drifted FURTHER from the page: line 22's "Settings > Audio > Earcons" path does not exist (master switch is on Notifications, `SettingsDialog.xaml:1165-1168`), per-category controls exist nowhere, and line 11's "lasts for your current session" is wrong twice over — the quick-mute persists via an immediate config save on toggle (`KeyCommands.cs:1144-1157`) AND shutdown capture (`AudioOutputConfig.cs:550`); the doc's "temporary layer on top" claim is also false, since quick-mute and the Settings checkbox are the same single bit. Two tasks in one: a doc correction and a design decision (should a mute silently outlive the session?). Merge with #43 — they are one decision approached from two sides.
- **#42 — extract rigmeter.** Still in-repo at `tools/rigmeter/`. Untouched.
- **#43 — per-category earcon controls.** `EarconPlayer.cs` still has no enum of any kind — roughly 60 flat static methods, one global `EarconsEnabled` gate. The help page still promises the categories (#39). Untouched.
- **#55 — master test list in for-noel format.** No such artifact exists in `docs/planning/for-noel/`. The untested backlog it must cover has grown by the ten-track merge and the entire speech-intent day.
- **#56 — radio-bench session.** Still the blocker for #10 and #27. Nothing landed that changes it. Consider making #27 explicitly a section of this session rather than a sibling task.
- **#65 — externalize strings to JSON.** Correctly HELD. Confirmed no strings/utterances JSON exists anywhere, no runtime loader, all strings inline. Blocked on the file-organization workshop with Noel, as recorded.
- **#70 — repeat-last-message ring.** `_lastMessage` is still a single string (`ScreenReaderOutput.cs:54, 740`); the handler (now `KeyCommands.cs:1117-1124`) walks nothing. The SpeechIntent refactor added a coalescer, not a history — `_lastByKey` is per-key dedup state, cleared on urgent flush, unusable as history. Description accurate.
- **#73 — DSP controls have no explanation.** Confirmed: zero HelpText on the Processing controls (the Workshop XAML has none at all; the five code-behind SetHelpText calls are all mic-area). One framing update: the delivery vehicle is now #84's Ctrl+F1 HelpText surface, not the F1 ContextMap the task cites — so this is sequenced behind #84's focus-boundary fix. Writing the HelpText prose (peak-steady-with-loudness-climbing, per the task) can proceed in parallel.
- **#78 — offer the trace at the moment of failure.** Confirmed absent: the per-radio save failure says only "See the trace file for details" (`SettingsDialog.RadioProfile.cs:815-820`); the SaveForRadio self-heal is retry-and-trace with no user-facing hook (`RadioConfig.cs:502-530`); CrashReporter.vb is crash-path only. The task's verified-plumbing claims (AutoFlush, shareable live trace, SessionArchive) all hold. Belongs inside the diagnostic-log/reporting track with #18's remainder.
- **#79 — learn the connection path from a trend.** Description current (it was refreshed post-Track A and matches the code): `PathChain` bare list, no provenance (`RadioConfig.cs:189-201`), `LastSeenRemote` single bool (291), `EffectiveChain` naive (`RigSelectorDialog.xaml.cs:74-85`). One accelerant the task does not know about: `Radios\ConnectionHistory.cs:9-49` already records a 10-entry per-radio ring of timestamped path/outcome/duration to `radios\{serial}\connect-history.json`, with the two policies this task wants ("connected remotely three times, switch the default?") named in its header as deliberately out of scope. The substrate exists; nothing reads it. Start there.
- **#83 — the five kept warning categories.** Description authored after the 2026-08-17 triage (3455 sites to 992, commit `0bc4ee20`) and only one day of commits followed; the counts are as fresh as anything in this backlog. Investigation-first framing intact and correct.
- **#89 — Shift+Tab out of the radio list.** The reopened 2026-08-18 body matches HEAD exactly: both workaround handlers removed (`808127d8`), only the empty-list Tab route remains (`RigSelectorDialog.xaml.cs:545-555`, fires only at Items.Count == 0), window Cycle set (`JJFlexDialog.cs:43`), `IdentityExpander` collapsed-by-default at `RigSelectorDialog.xaml:180` as the leading suspect. Per the owner: no fifth fix — bisect. One cheap piece of new evidence available first: temporarily start the IdentityExpander expanded and press the key; that discriminates the leading suspect in one build without touching navigation code.

## DRIFTED — 3 tasks. Work outstanding, description would mislead.

- **#12 — paWinWasapiAutoConvert.** The code claim holds — no `PaWasapiStreamInfo` binding exists anywhere, both stream builders null `hostApiSpecificStreamInfo` (`Audio.cs:112`, `MicProbe.cs:430`) — but the framing is stale twice. First, the queue says "decide with #61": #61 is decided and shipped, so this task stands alone now. Second, the decision RAISES the stakes: WASAPI is now the default host API (`Devices.cs:624`), so the shared-mode rate refusal sits on the default path instead of an opt-in one. Mitigation to test against: the rate-negotiation fallback ladder (`Audio.cs:236`, 48000 down to 8000 including 44100) may already absorb the refusal by opening at the device's native rate. Corrected framing: with WASAPI default, determine whether the negotiation ladder makes AutoConvert unnecessary or whether a 44.1-locked device still fails to open — and only then bind the struct.
- **#17 — decoded PC-audio arrives quiet.** The description's anchor ("4.0x was hardcoded") is gone: the boost is now the operator-facing `PcOutputVolumeDbSetting`, 0-24 dB, default 12 (`FlexBase.cs:9483-9501`, applied at 12066). The investigation is untouched and has a new, sharper lead this audit surfaced: the stale comment claiming the Opus path "bypasses FlexLib's RXGain scalar" (12060-12062) is contradicted 330 lines later where the code sets `RXGain = 50` on the very same channel (12394). If 50 is a mid-scale attenuation, that could be a large part of the missing level — check what FlexLib does with that scalar before touching anything else, then re-measure (the queue's own advice), remembering the coupled default: if the source arrives hotter, the +12 default becomes too loud in the same release.
- **#85 — local connect announces SmartLink activity.** The work stands but the prime suspect in the body is probably wrong. The suspected `AnnounceLoadedState` chatty line still exists (`RigSelectorDialog.xaml.cs:777-782, 2018-2022`, "Remote connection list loaded", verbosity-gated, non-interrupting) — but the stronger candidates are the auto-started remote pass: `AutoStartRemote` speaks "Starting remote radios for your account." on Loaded (649-656) and `StartRemoteFlow` speaks "Connecting to SmartLink as {email}." (1961-1968) — unconditional, NOT verbosity-gated, via the legacy ungated overload, plus a "Connecting to SmartLink..." window. That is literal SmartLink language on a local connect whenever the per-account opt-in (`SmartLinkAccountManager.cs:966-974`) is set. The task's own discipline still applies — it warns an attribution was already made wrongly once by plausibility — so keep the verbose-trace confirmation step, but point it at the AutoStartRemote chain first. Note also the enum doc (`ScreenReaderOutput.cs:27-31`) classifies account/discovery chatter as Diagnostic-class, which 1961-1968 ignores.

## UNVERIFIABLE — 1 task. Code cannot settle it; here is what would.

- **#59 — four slices on connect, one in FM.** The code half is now settled and it exonerates the app: nothing in JJ Flex creates a slice on connect — no `RequestSlice`/`slice create` caller exists in `Radios\` (the only creators are FlexLib internals and `FlexBase.NewSlice` at 11580-11620, whose call sites are all operator actions plus the loopback "ears" slice inside `StartLoopbackArrangement`, `FlexBase.cs:10519`). The connect path only subscribes and enumerates (`FlexBase.cs:1141-1142, 6952-6997`). So "most likely a regression" is unsupported; `NewSlice`'s own comment anticipates the real explanation — a radio profile filling all slots at startup (11582-11584). What settles it: at the bench, check the 8600's resident slices from SmartSDR or after a radio reboot BEFORE connecting JJ Flex. If four slices pre-exist, close this; if they appear only after a JJ Flex connect, the loopback arrangement is the first thing to trace. Fold the check into #56's bench session.

## SUPERSEDED — none.

The one candidate, #90, is not superseded — see its PARTIALLY DONE entry. The limited-Home decision (for which no written record exists in the repo or project memory — worth capturing before it drifts too) reshapes Home, but the Audio Workshop sits ON the limited page's menu and is deliberately usable offline, so per-control truthfulness inside the Workshop survives the decision.

## Tasks that are really two tasks, and pairs to merge

- **#18** is a dialog-UX fix plus a reporting-pipeline build. The dialog is being retired by the ratified design; split, close the first half, and fold **#78** into the pipeline half.
- **#57** is a shipped TX-rate setting plus an unstarted DAX IQ stream. Split; close the first half.
- **#39 + #43** are one decision from two sides (fix the page or build the categories) — merge, and #39 additionally hides a second decision (quick-mute persistence) that needs Noel either way.
- **#44 + #51**: #51's radio-side half shipped; close #51 and let #44 carry the operator/rig split alone.
- **#12** should be decoupled from the closed #61 and reframed per its DRIFTED entry.
- **#27** is the transverter section of **#56**; #10 is the coding task gated on #56's other section. Consider one bench-session task with two sections and two downstream coding tasks.
- **#58** is two defects; the garble half is fixed, leaving the connect announce-storm and the duration-estimate player as the remainder.
- **#84** is arguably three: the Ctrl+F1 surface (shipped, currently suspect), coverage, and the never-built F1 ContextMap mechanism plus its doc contradiction.

## Bookkeeping drift found along the way

- The task store marks #9, #26, #32, #40, #49, #50, #51, #61, #68, #71, #74, #75, #76 as pending; all are done in code. #86 is correctly in_progress; #89 correctly reopened.
- `research-queue.md` says #41 "NOT started" (an `authors` subcommand exists), describes #68's Save as write-both (it is canonical-only now), and still lists #61, #74, #75 as open.
- CLAUDE.md's trace-file section points at `TraceAdmin.vb` for the Help > Tracing dialog; the menu actually opens the WPF `TraceAdminDialog` (`NativeMenuBar.cs:1646-1667`). The VB form is legacy and its toggle has no explicit AccessibleName (`TraceAdmin.Designer.vb:386-394`).
- The About tab's second version assembler (`AboutDialog.xaml.cs:202-229`) violates the single-assembler rule its own DiagnosticSnapshot declares — a small but exact instance of the project's dominant defect class, in the component built to fight it.
