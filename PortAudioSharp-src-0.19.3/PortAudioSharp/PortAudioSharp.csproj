<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Platforms>x86;x64</Platforms>
    <Nullable>disable</Nullable>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <None Include="app.config" />
    <!-- Removed 2026-08-12: the binding project bundles its own
         portaudio.dll (V19.6.0-devel, 2016) for standalone users. Copying it
         to our output root shadowed runtimes/win-<arch>/native/ and created a
         silent-downgrade path. Natives come from runtimes/ only. -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\\..\\P-Opus-master\\POpusCodec.csproj" />
  </ItemGroup>
</Project>
